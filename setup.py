from setuptools import setup, find_packages

setup(
    name="tg_clientes",
    version="0.1",
    packages=find_packages(),
    install_requires=[],
    include_package_data=True,
    description="Módulo principal de la aplicación TurboGears",
)
